<%= form_for @stop.nil? ? :stop : @stop do |f| %>
  <p>
    <%= f.label :stop_id %><br>
    <%= f.number_field :stop_id %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Back', stops_path %>